import 'package:flutter/material.dart';

class ChatSessionsList extends StatelessWidget {
  const ChatSessionsList({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    // TODO: реализовать список сессий чата
    return Container();
  }
}
