import 'package:flutter/material.dart';

class LinkPreview extends StatelessWidget {
  const LinkPreview({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    // TODO: реализовать предпросмотр ссылки
    return Container();
  }
}
