import 'package:flutter/material.dart';

class SubscriptionComparison extends StatelessWidget {
  const SubscriptionComparison({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    // TODO: Реализовать сравнение планов подписки
    return Container(
      padding: const EdgeInsets.all(16.0),
      child: const Text('Subscription Comparison'),
    );
  }
}
