import 'package:flutter/material.dart';

class SubscriptionCTA extends StatelessWidget {
  const SubscriptionCTA({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    // TODO: Реализовать call-to-action для подписки
    return ElevatedButton(
      onPressed: () {},
      child: const Text('Оформить подписку'),
    );
  }
}
