import 'package:flutter/material.dart';

class BenefitsList extends StatelessWidget {
  const BenefitsList({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    // TODO: Реализовать список преимуществ подписки
    return Column(children: const [Text('Benefits List')]);
  }
}
