import 'package:flutter_test/flutter_test.dart';
// Импортируйте необходимые виджеты и провайдеры

void main() {
  group('ChatScreen Widget', () {
    testWidgets('отображает отправленное и полученное сообщение', (
      tester,
    ) async {
      // Arrange
      // Act
      // Assert
    });

    testWidgets('корректно отображает индикатор загрузки', (tester) async {
      // Arrange
      // Act
      // Assert
    });

    testWidgets('отображает ошибку при сбое', (tester) async {
      // Arrange
      // Act
      // Assert
    });
  });
}
