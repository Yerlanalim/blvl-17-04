import 'package:flutter_test/flutter_test.dart';
import 'package:mocktail/mocktail.dart';
// Импортируйте необходимые зависимости context_builder

void main() {
  group('ContextBuilder', () {
    setUp(() {
      // Инициализация моков и зависимостей
    });

    test(
      'должен корректно формировать контекст для простого диалога',
      () async {
        // Arrange
        // Act
        // Assert
      },
    );

    test('должен оптимизировать контекст для длинных диалогов', () async {
      // Arrange
      // Act
      // Assert
    });

    test('должен корректно обрабатывать edge-cases', () async {
      // Arrange
      // Act
      // Assert
    });
  });
}
